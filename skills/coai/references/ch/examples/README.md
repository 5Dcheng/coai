# 示例

## 作用

这个目录提供 CoAI 资产的示例文件。

示例用于帮助理解“长什么样”“信息浓度如何控制”“字段如何对齐”。

## 使用边界

- example 用于理解，不是正式生成模板
- 只有在 template 不够具体时，才读取 example
- 不要把 example 原样复制进真实仓库而不做改写

## 文件说明

- `feature-example.md`：功能文档示例
- `mapper-example.json`：mapper 示例
- `node-example.json`：node 示例
